= simple_form_for(@payment_type, :html => { :class => 'form-horizontal' }) do |f|
  
  = f.input :name

  %div{class: "form-actions"}
    = f.submit nil, :class => 'btn btn-primary'
    = link_to "Cancel", payment_types_path, :class => 'btn'